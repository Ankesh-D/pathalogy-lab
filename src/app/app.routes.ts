import { Routes } from '@angular/router';
import { TestMasterComponent } from './features/masters/test-master/test-master.component';

export const routes: Routes = [
    {
        path: "a", component: TestMasterComponent
    }
];
